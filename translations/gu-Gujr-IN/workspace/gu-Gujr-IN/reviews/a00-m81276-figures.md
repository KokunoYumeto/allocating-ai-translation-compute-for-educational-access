# A00 m81276 figure-localization review

## Durable goal and frozen authority

This bundle closes the visual-language and figure-accessibility portion of Gujarati module `m81276`. The authoritative source is `downloads/gu-Gujr-IN/a00-id/provenance/logbook/authority/prealgebra2e/m81276.source.cnxml` at SHA-256 `7e60eb505941fae754150df495be0ff7acf303c355eb1e81a8c57d48090e9cef`. The frozen Gujarati translation is `gu-Gujr-IN/translations/a00-m81276.gu.cnxml` at SHA-256 `f77dfe5ab67eb6db2a50e372d0390cbf8a587da4e6e0016d9b4eb63a55e12614`; its source-bound media/errata handoff is `gu-Gujr-IN/translations/a00-m81276-media-and-errata.gu.json` at SHA-256 `9a91525b24b1b73bea1deea5ac4a0ee1f42d8ad6bd32d9e10c0981d12dc84e51`. These files, the source numbers, the existing faithful alternatives and the keyed source errata are immutable inputs here. This task writes only the independent renderer, its QA script and review receipts. It does not alter the shared reader, dispatcher, translation, metadata, status, terminology or source-error handling.

The source contains 72 media occurrences and 71 unique assets; `CNX_BMath_Figure_03_02_029_img-03.png` occurs twice under distinct media IDs. Every unique original was opened at original resolution, first for classification and again during revision. Twelve unique originals contain English and therefore receive semantic Gujarati HTML/SVG redraws. Fifty-nine unique originals contain only counters, arrows, grouping marks, variables, numerals, operators, mathematical punctuation or standard mathematical notation and return `None`, preserving their exact raster artwork. That yields 12 redraw occurrences and 60 verified-original occurrences. No asset is classified from its filename or source alt alone.

## Source and canon consultations

Before drafting, the actual source CNXML, frozen Gujarati CNXML, all 72 metadata records, all 71 distinct original files, `AGENTS.md`, `USER_INSTRUCTIONS_VERBATIM.md`, `gu-Gujr-IN/terminology.csv` and `gu-Gujr-IN/canon/examples.csv` were read. The same source, mapping and twelve language-bearing originals are read again during revision and output review. Terminology decisions used here are: integer `પૂર્ણાંક`, positive number `ધન સંખ્યા`, negative number `ઋણ સંખ્યા`, variable `ચલ`, expression `પદાવલી`, equation `સમીકરણ`, and neutral pair `શૂન્ય બનાવતી જોડી`. Variables remain Latin mathematical variables.

Two indexed-readable Khan Academy Gujarati transcripts were read in full as primary current canon, rather than inferred from search titles: `https://gu.khanacademy.org/math/arithmetic/arith-review-negative-numbers/arith-review-add-negatives-intro/v/adding-negative-numbers` and `https://gu.khanacademy.org/math/arithmetic/arith-review-negative-numbers/arith-review-add-and-sub-integersss/v/negative-number-practice`. They support `ઋણ સંખ્યાઓનો સરવાળો`, `સંખ્યારેખા`, signed addition/subtraction and left/right movement. Their examples are consultation evidence only; they do not replace the pinned OpenStax source or its values. The Indonesian counterpart remains a candidate comparison and is not used to replace any source text, diagram or arithmetic.

## Redraw inventory and model checks

The legend `024` preserves one light-blue positive and one red/pink negative counter and translates only the labels to `ધન` and `ઋણ`. Counter labels `025-03`, `026-03`, `027-04`, and `028-04` preserve respectively 8 blue positive counters, 8 red negative counters, 2 red negative counters, and 2 blue positive counters, with visible labels `8 ધન`, `8 ઋણ`, `2 ઋણ`, and `2 ધન`.

Substitution captions `029-01` through `034-01` preserve the source variables, values, operators and color scope. The full signed values −2, −11, −5, −5 and −18 are red where the artwork uses red; in `033-01`, 12 is red and the full signed value −30 is cyan; in `034-01`, −18 is red and 24 is cyan. Independent arithmetic checks bind the paired source steps: −2+7=5; −11+7=−4; −5+1=−4; −(−5)+1=6; 3(12)+(−30)=6; and (−18+24)^2=36. The prompt is a caption for the adjacent retained mathematical step and does not silently redraw or change it.

`CNX_BMath_Figure_AppB_014.jpg` becomes five semantic one-skill tables, each with three `scope=col` response headings. It preserves all five skill statements, the three response labels per skill and all 15 intentionally blank response cells. Each blank cell has the Gujarati accessible label `ખાલી`; no response is invented.

The original-image review confirmed the source-alt discrepancies already keyed in the frozen handoff: swapped color/meaning descriptions for `016` and `022`; the bottom five counters in `017` are positive; `039` depicts 3+4; `041` depicts 4+(−6); `044` depicts −3+7; `045` has eight negative counters; `046` has seven negative counters; `207` has seven red negative counters in its lower row; and `036-03` contains no ellipsis. These findings are not patched in this helper because those assets are exact mathematical-original retentions and root owns the keyed accessible correction layer.

## QA and output review

The source-bound QA must prove all pinned hashes, the 72 occurrence order and media IDs, every original asset hash, the 71-asset unique set, the exact 12/59 classification, and the duplicate occurrence. It rejects multi-letter Latin prose in rendered visible text, checks globally unique SVG/ARIA references, validates the counter quantities and signs from semantic data attributes, validates all six substitution calculations independently, and validates the five skills, 15 scoped headers and 15 blank cells. Three local preview pages isolate counter figures, substitution captions and self-check tables.

Phone review uses a 390-pixel emulated viewport, whose content width is 375 pixels with the scrollbar. Desktop review uses a 1000-pixel emulated viewport, whose content width is 985 pixels. Each preview page must satisfy `scrollWidth == clientWidth`, keep every localized wrapper within the viewport, show Gujarati text with the bundled Gujarati font, and retain all blank self-check cells. Representative screenshots are kept only as browser evidence in the local build preview directory; the durable browser receipt records viewport geometry and visible checks. The last output review reruns the hash-bound QA after all edits and records the renderer, QA, review and receipt hashes before ownership is returned.

## Uncertainties and localization boundary

Counter colors are semantic and follow the actual artwork: pale blue means positive and pink/red means negative in this module. The redraw does not claim color alone as the accessible distinction; Gujarati labels and the surrounding alternative remain present. The word `કાઉન્ટર` is retained because the source teaches a counter model and the frozen Gujarati translation already uses it. Mathematical-only originals may contain single Latin variables; those are mathematical tokens and are not translated. The helper returns `None` for unknown filenames as a safe integration boundary.
