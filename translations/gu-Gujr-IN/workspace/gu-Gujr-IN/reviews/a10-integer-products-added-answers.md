# All39 omitted A10 m82455 answers — root review — 2026-09-01

This separate companion covers every source exercise that omitted a solution, without changing the faithful Gujarati CNXML. Root read all39 original problem subtrees and their shared instructions. Pinned source SHA256: `794635f93249017847f2646910d007e9de53b00ee6037133aa5c3edb7c2b88ec`. The companion preserves source order and binds every answer to its original exercise ID.

Coverage includes seven direct products/divisions, nine operation-order and power expressions, nine substitutions, seven phrase-to-expression items, five source-context applications and two explanatory prompts. Unary minus remains outside the base in `−6²`, while `(−3)⁵`, `(−4)²` and `(−4)³` keep the signed base inside parentheses. The final explanation establishes both sides of `−4³ = (−4)³` as `−64` and gives the even-power counterexample `−4² = −16` versus `(−4)² = 16`.

The symbolic quotient is `−7 / (m + n)` with the necessary domain condition `m + n ≠ 0`; it does not split the denominator or invent values. Distribution gives `−13(c − d) = −13c + 13d`. The division-rules response covers like and unlike nonzero signs, a zero dividend, the undefined zero divisor and a non-integer quotient example. It is labelled as a sample response.

Source contexts stay bounded to the textbook facts. The January21 temperature problem retains `89°` and `−31°` and reports a `120°` difference without inventing Celsius/Fahrenheit or current weather. The Steelers problem distinguishes the net `−7`-yard change from the final own `23`-yard line. Account balances are `−$42` and `$232`, with no invented fees. The diet-program exercise treats the given average as a total `−24`-pound change; it does not claim every participant lost exactly3pounds or give health advice.

The machine QA parses the original MathML and prose, binds43 original question-math parts, independently evaluates source expressions with exact arithmetic and checks106 displayed equality links. It also parses the actual generated HTML and binds all39 questions, complete step lists, final answers and source backlinks. Receipt: `A10_INTEGER_PRODUCTS_ANSWERS_QA.json`.

Actual output review: root read all39 rendered question/step/answer blocks in three browser-DOM batches1–13,14–26 and27–39. At the phone override, `innerWidth=390`, document client/scroll widths were375/375 and the local Gujarati font was loaded. At desktop1265, client/scroll widths were1250/1250 and the main reading column occupied860pixels. All39 backlinks were present. Screenshots inspected the `−6²` grouping at phone width and symbolic questions31–32 at desktop width. These are browser/render checks, not actual assistive-technology or native educator acceptance.

Canon and terminology: retained the61-entry ledger, initial13 examples, already-read Gujarati multiplication/division evidence and the established distinctions `ગુણાકાર`/`ગુણનફળ`, `ભાગાકાર`/`ભાગફળ`, `ભાજ્ય`/`ભાજક`, `ધન`/`ઋણ`, `વિરોધી સંખ્યા`, `આધાર` and `ઘાતાંક`. During this companion review root read the visible indexed Gujarati signed-multiplication transcript for positive/negative sign cases, repeated addition, commutative order, negative-times-negative and multiplication by zero. Its noisy spelling and examples were language witnesses only; all current quantities were checked against the pinned source. No native Gujarati approval is claimed.

Companion SHA256: `6190598a3741d418ba0c27c7b21ffbf9125192806e930cdd7a28198b44f6106e`. Reader SHA256: `2f05b01cb560da2079b18cac6f306fe672abb707c82647033ecb66e5317ed825`. Independent peer review recomputed37 determinate items, checked the two source-open responses and bound all39 old-hash HTML blocks. It found two narrow Gujarati issues: the distributive-property label and Reymonte transliteration. Root applied both, reran source/equality/render QA and reread the changed32nd and36th blocks in the actual reader; the peer independently rebound all39 current-hash blocks; the final receipt now passes with no remaining finding. The full157-module assignment and AX-1/AX-3 workflows remain active.
