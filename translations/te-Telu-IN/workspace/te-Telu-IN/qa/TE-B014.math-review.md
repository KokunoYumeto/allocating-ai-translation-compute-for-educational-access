# TE-B014 independent mathematical review

Completed content/math review, 2026-08-31. No mathematical findings remain in
the hash-identified human files and assembled target below. This receipt covers only **Model Addition of
Whole Numbers**, frozen root `m81244#fs-id2145437`. The validator author did not
write this unit's translation, bridge or assets. This is a bounded mathematical
review, not native-speaker certification or final integrated-reader visual QA.
The full assignment remains active.

## Evidence read and source inventory

Read the complete frozen English subsection, the complete corresponding
Indonesian subsection, source metadata and all19 complete original images.
Indonesian is a comparison witness; frozen canonical English governs the unit.
Read the manifest as an asset mapping/receipt, not as an answer oracle. The
checker independently counts actual SVG rectangles and checks their arrangement.

- Canonical commit: `38cae454e644abf9f0a623e876994553881597c9`.
- Frozen source SHA-256:
  `b865a80cc39efa14f98ddd39d05d2ff688439978b39d9e153533254c9ad91352`.
- Source:322 elements,171 slots,86 IDs,30 MathML roots,19 media/images,
  four tables, three worked exercises and six TryIts. The introductory3+4
  example makes ten distinct source operand pairs, not ten exercise nodes.
- All nine original solution IDs must remain in the translated CNXML.
- Next source sibling is `fs-id1385496`, **Add Whole Numbers Without Models**;
  it is outside this review.

| Source role / exercise ID | Original solution ID | Exact sum | Combined tens, ones → final tens, ones |
| --- | --- | --- | --- |
| Intro, `eip-167` / `fs-id1607848` | Narrative |3+4=7 |0,7 →0,7 |
| Worked, `fs-id1471255` | `fs-id1969376` |2+6=8 |0,8 →0,8 |
| TryIt, `fs-id2792546` | `fs-id1833338` |3+6=9 |0,9 →0,9 |
| TryIt, `fs-id1886370` | `fs-id1241970` |5+1=6 |0,6 →0,6 |
| Worked, `fs-id1792383` | `fs-id1471782` |5+8=13 |0,13 →1,3 |
| TryIt, `fs-id2265162` | `fs-id2140822` |5+7=12 |0,12 →1,2 |
| TryIt, `fs-id1863239` | `fs-id2129995` |6+8=14 |0,14 →1,4 |
| Worked, `fs-id1939376` | `fs-id1966628` |17+26=43 |3,13 →4,3 |
| TryIt, `fs-id2137714` | `fs-id2483638` |15+27=42 |3,12 →4,2 |
| TryIt, `fs-id2813270` | `fs-id2267850` |16+29=45 |3,15 →4,5 |

For each pair, integer `divmod` determines each addend's tens/ones, the combined
counts, exchanged tens and remaining ones. No floating-point calculation or
Python rounding is involved. Every exchange removes ten ones for each added
ten-rod; initial and final values are equal. Original source images supply all
six TryIt answers, including the equations that some English alts omit.

## Actual canon consultation and application

Read existing OCR first, then inspect the complete corresponding page image.
No new downloads, OCR, PDF extraction or corpus copy was needed. The existing
Telugu terminology table was reread for the whole-number/ones/tens distinctions.

1. TS Grade2, PDF20 / printed8, chapter2 exercise1 (`C01`): bundles and loose
   sticks, the supplied10+1=11 and10+2=12, partly blank intervening rows, and
   the final ten loose sticks exchanged into a second bundle,10+10=20.
   Application: the exchange changes representation, not value; count ten
   cells inside a rod, and do not add before-and-after pictures together.
   This is not a claim that every intervening row is already solved.
2. TS Grade2, PDF33 / printed21, exercise4 (`C05`): the worked first row
   explicitly reads four tens plus one one,40+1=41. The OCR misreads41 as
   `Al`; the full image resolves it. Application: keep the number of rods
   separate from their contribution (four rods versus forty). Check every
   equality side and both stage counts, even when a coincident total is right.

The PDF skill's OCR/image workflow affected these conservation and count/value
checks. These are Telangana witnesses, not Andhra Pradesh evidence. No official
AP terminology, formal term attestation beyond the actual pages, or native
speaker approval is asserted.

Selected evidence SHA-256:

| Existing selected file | SHA-256 |
| --- | --- |
| `downloads/canon/ocr/TS-p020.txt` | `712e24e4c7466c792941d717b7d9f6b00994f87803a4ceb3ec0df64120178588` |
| `downloads/canon/ocr/TS-p020.png` | `c4f878592e6154c62a06323e626a6bc6c7683f75b75e5de9bfc4de2e12fb6d4e` |
| `downloads/canon/ocr/TS-p033.txt` | `2f7659b95b9e6c1a9c04c99928c4bef1b32030c7922fdb1ef041668e2f509994` |
| `downloads/canon/ocr/TS-p033.png` | `8bed0c3e8b1b3ffd1d042dff1853d7851a76a79c51ccbb7af5995a15452e7d95` |

## Source exceptions and model inspection

- `eip-951` retains the source declaration `cols="3"`, although its three
  actual rows have two entries each. This requires the main renderer's explicit
  exception, not mutation of the source/target CNXML. The other tables are
  `eip-167`:3×2, `eip-555`:5×2, and `eip-93`:5×3.
- `eip-id1168289744408`, original `018_img-04.png`: three rods are on the
  **left** and thirteen ones on the right; ten ones are red. The English alt
  incorrectly puts both on the right. The corresponding Indonesian alt and
  actual original pixels agree on left/right. Retain the source error only in
  frozen English evidence; the final target description correctly states left
  rods/right ones. The catalog records the correction explicitly.
- Original `017_img-04.png` has ten enclosed ones plus three outside before
  exchange, then one ten-rod and three units after. Both states are13. Counting
  all rectangles in both drawings as a single quantity would wrongly give26.
- Original `018_img-04` / `018_img-05` likewise represent the same43 before
  and after exchange, not86. The individual source models17 and26 precede this.
- Preserve the raw-text source equation5+8=13, which is not in MathML, as well
  as all30 MathML roots including `base-10`, `(=)`,3+4=7,2+6=8,40+3=43 and
  17+26=43.
- The source asks learners to **model** addition. A bare numerical answer does
  not replace the model/reasoning task. The worksheet activity note names an
  activity but supplies no file or URL; this review does not claim it acquired.

## Actual translation and bridge review

Read the full catalog, every assembled target paragraph/table entry, all19
target alts and all four accessible table descriptions. Read the complete
bridge, then reread its final changed K3 caption and last-two-rows warning.
Final catalog polish only added spacing and clarified rod cells as చిన్న గడులు;
the actual resulting descriptions were reread. All19 complete unchanged
original images were also inspected at final review, not merely their alts.

The target preserves the modeling request, block value1 versus rod value10,
the strict addend-less-than10 premise, the **inclusive**10-or-more exchange
boundary, the two-equal-values meaning of equation, and the raw5+8=13 cell.
All322 elements,86 source IDs,30 MathML roots and nine original solution IDs
remain. The two-digit text, accessible descriptions and actual SVG cell counts
agree on17,26, combined3tens/13ones, and regrouped4tens/3ones. The compiler's
image mapping is checked against every original source filename, so two images
with the same value cannot be silently interchanged.

The bridge has33 unique IDs,20 correctly targeted local links and12 solution
details: all six source TryIts plus three original entry checks and three
original rechecks. Every source solution has its specific source-exercise
backlink. The diagnostic routes are D01→K1→R01, D02→K2→R02 and D03→K3→R03.
These are instructional routing aids, not validated grade classifications.

| Original check | Pair/result | Combined tens, ones → final tens, ones |
| --- | --- | --- |
| D01 |2+4=6 |0,6 →0,6 |
| D02 |7+3=10 |0,10 →1,0 |
| D03 |18+24=42 |3,12 →4,2 |
| R01 |4+0=4 |0,4 →0,4 |
| R02 |6+7=13 |0,13 →1,3 |
| R03 |27+13=40 |3,10 →4,0 |

All43 displayed equation chains (44 equality relations) were recomputed from
actual displayed text, including unscored support paragraphs and table cells.
All12 strong final answers retain their exact operands and order; actual
before/after count statements, equations, exchanged-unit counts and English
number glosses agree. Correct arithmetic alone is not enough:15+27 cannot
be replaced by18+24 merely because both equal42, nor can3tens/12ones be called
4tens/2ones at the wrong stage. Source5+7 uses five from the second group,
leaving two;6+8 uses four, leaving four. These verbal partition explanations
are checked separately from final totals.

The bridge explicitly preserves value when exchanging blocks, forbids counting
the replaced ten units again, allows exactly ten units, retains zero ones in40,
and does not draw a value-one block for a zero addend. The K3 table's first two
rows are the distinct addends17 and26; **only its last two rows** are alternate
representations of43. Its final caption no longer suggests all four rows have
one value. The worksheet note truthfully says no worksheet/link is supplied;
the original support does not claim to reproduce or complete it.

## Validator and read-only regressions

Integration signature:

```python
from addition_model_checks import validate_b014
checks = validate_b014(target, bridge)  # actual ElementTree roots
```

Command:

```powershell
python -B -m unittest discover -s te-Telu-IN/scripts -p test_b014.py
```

Final result: **86 tests pass, zero skips**. These are real mutations of the
actual target/bridge/SVG trees, not only comparisons of fixed answer constants.
The suite corrupts each of the43 actual equation occurrences; every one of12
strong answers, state-count sequences and English quantity glosses; all six
source backlinks; all nine source problems/solution IDs; all19 target alts and
model mappings; the four accessible table descriptions; and the table/diagram
count-versus-value roles. It also covers exact-ten and zero boundaries,
missing/extra units, unit scale, contiguous ten-rod geometry, overlapping
cells, exchange stage separation, wrong-but-coincident totals, altered modeling
requests, missing solutions, swapped stages, routes and worksheet-scope claims.

A before/after SHA-256 snapshot of45 bounded files (source/metadata, catalog,
bridge, the two checker scripts, manifest,19 original images and19 SVGs) was
identical across the final test run. Tests use in-memory corruptions and write
no generated artifacts. The review receipt itself was finalized afterward.

Final fingerprints, SHA-256; paths relative to `te-Telu-IN/`:

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `translations/TE-B014.te.json` |19832 | `eb689d4fb3ed6e7b6699d4a439f66073abe491af836e041a4515ed38d1a32ab5` |
| `translations/TE-B014.bridge.xhtml` |30670 | `493a7cba14a438f5631780e39ffb5661e03e8543df6c65057c891de29cdb08f7` |
| Assembled target, serialized in memory |24186 | `34a9ea00b6f11293b72ab66d558e7e2dd6712ce2f5f65c3e354d2ed383764aec` |
| `scripts/addition_model_checks.py` |35120 | `720c38ed918453761627a158ca9f61072d8bff8a47ae3f5eda32fdc48f17ccaa` |
| `scripts/test_b014.py` |34865 | `a40f21bda44bc041fb68a4dedf6043075739e93ed3ae43035944c8529b8c7f95` |
| `assets/B014/manifest.json` |31885 | `b2d8cb1412880053c2d13b06509b099c504cca0a3d31c6d6594dc88c70809f6f` |

At this receipt's content handoff, `generated/TE-B014.te.cnxml` does not yet
exist. The exact real catalog and source were localized in memory, fully
checked, serialized and hashed above; no on-disk generated-target read is
claimed. Main owns compiler integration, the eip-951 rendering exception and
final integrated reader/visual QA. The independent review here covers actual
content, original image pixels and SVG mathematical geometry, not final SVG
render screenshots or final reader layout. No source, translation, asset,
shared builder, registry or log was edited by this review task.
