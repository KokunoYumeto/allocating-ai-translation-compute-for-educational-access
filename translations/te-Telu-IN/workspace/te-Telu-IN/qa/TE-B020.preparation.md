# TE-B020 remainder-source preparation

Prepared 2026-09-01. This receipt covers only the exact remaining source graph of canonical A00 module `m81244` after TE-B011 through TE-B019. It does not translate, build or render anything and does not claim that the translated module, A00 or the wider commission is complete.

## Pinned module proof

| Artifact | Bytes | SHA-256 / Git blob |
| --- | ---: | --- |
| `downloads/upstream-prealgebra/modules/m81244/index.cnxml` | 119,141 | SHA-256 `b32058ce714e5fd43b010ccc81b2ae00a11567b229e9f96dda7b85b3cd82ba6b`; Git blob `1c3617fd0547029c883126348113f437395d718b` |
| `sources/TE-B020.en.cnxml` | 887 | SHA-256 `bda74b9e18a5bb466709b01e569d6511c265606dd1c0eefd178c1815064ac91d` |
| `sources/TE-B020.source.json` | 5,344 | SHA-256 `948b3a8db2b9fa894c48e12cfffc01b59bb2ac9920d538e2b15050a7c9c5c32c` |

The canonical repository pin is commit `38cae454e644abf9f0a623e876994553881597c9`. The 119,141 module bytes match both the source-lock SHA-256 and the pinned Git blob computed over `blob <length>\0<bytes>`.

The canonical direct-child order is exactly `title`, `metadata`, `content`, `glossary`. The content element has exactly nine direct child subtrees, in this order:

1. note `fs-id2299412` → TE-B011
2. note `fs-id1122444` → TE-B012
3. section `fs-id2601285` → TE-B013
4. section `fs-id2145437` → TE-B014
5. section `fs-id1385496` → TE-B015
6. section `fs-id2691382` → TE-B016
7. section `fs-id2197427` → TE-B017
8. section `fs-id1611455` → TE-B018
9. section `fs-id2263283` → TE-B019

Each frozen B011–B019 root was compared structurally with its corresponding canonical child after removing only the selected root's exterior tail. Every nested tag, ID, attribute, text/tail value and child order matches; its byte hash, element count and slot count also match its source metadata.

## Exact B020 selection

B020 is an explicit document-remainder partition, not a synthetic lesson section. It preserves:

- the original `document` wrapper, with no invented ID or attribute;
- the exact source title `Add Whole Numbers`;
- the complete metadata subtree: protected content ID `m81244`, metadata title, introductory five-objective abstract, UUID `8069044b-6fb1-49bf-b03a-64988f9b1ddd`, and the original `para-00001` / `list-00001` IDs;
- the original `content` wrapper's tag, attributes, text and tail, with its nine already-frozen child subtrees absent so they are not duplicated; and
- the complete glossary subtree: definition `fs-id1226736`, term `sum`, and meaning `fs-id1245763`, “The sum is the result of adding two or more numbers.”

Thus “opening” means the source title plus the complete metadata/objectives; it is not separate prose outside the module. “Glossary” means the one exact canonical definition, not a newly titled or editorial section. The `@module-remainder` value exists only in metadata as a future catalog sentinel and was not inserted into the XML.

The frozen document has 19 elements, 12 text/accessibility slots and four IDs, all unique:

```text
para-00001
list-00001
fs-id1226736
fs-id1245763
```

Slot `s002` is the protected canonical content ID and slot `s010` is the protected UUID. The other slots are the title, metadata title/opening/objectives, glossary term and glossary meaning. There are no media, images, figures or MathML nodes, so no asset work is warranted.

XML namespace declarations were normalized during UTF-8 serialization, as in the existing auxiliary freezes. At the XML information-set level, the complete title, metadata and glossary signatures equal the canonical source, and the retained content wrapper's tag, attributes, text and tail equal the canonical wrapper. Only the nine explicitly enumerated content child subtrees are excluded.

## Coverage arithmetic

The canonical module contains 3,576 elements and 1,958 enumerated text/accessibility slots.

| Partition | Elements | Slots |
| --- | ---: | ---: |
| TE-B011–TE-B019, all nine content child subtrees | 3,557 | 1,946 |
| TE-B020, wrapper/title/metadata/content wrapper/glossary remainder | 19 | 12 |
| Reconstructed canonical module graph | **3,576** | **1,958** |

The element equation is `3,557 + 19 = 3,576`; the slot equation is `1,946 + 12 = 1,958`. The extra element outside the nine content subtrees is the retained `content` wrapper itself. The remaining 18 elements are the document wrapper, title, 12-element metadata subtree and four-element glossary subtree.

The content-subtree arithmetic also agrees internally:

```text
elements: 18 + 13 + 143 + 322 + 1480 + 273 + 210 + 81 + 1017 = 3557
slots:     8 +  6 +  93 + 171 +  859 + 148 + 114 + 53 +  494 = 1946
```

Therefore B011–B020 provide complete, non-overlapping **source-freeze coverage** of the `m81244` XML graph and enumerated slots. This statement is limited to source coverage: it is not a claim that translations, bridges, assets, generated readers, accessibility review or the module-wide deliverable are complete.

## Verification actually performed

A read-only verifier reparsed the pinned module, B020 and every B011–B019 source/meta pair. It checked the source-lock SHA, pinned Git blob, direct-child order, structural signatures, IDs, attributes, protected values, per-unit hashes/counts and both partition equations. Result:

```json
{"status":"PASS","module_elements":3576,"module_slots":1958,"covered_content_elements":3557,"covered_content_slots":1946,"b020_elements":19,"b020_slots":12,"selection_ids":["para-00001","list-00001","fs-id1226736","fs-id1245763"],"source_sha256":"bda74b9e18a5bb466709b01e569d6511c265606dd1c0eefd178c1815064ac91d"}
```
