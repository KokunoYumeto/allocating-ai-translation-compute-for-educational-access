# Source and notices

The canonical English source is OpenStax Elementary Algebra 2e at the exact official Git commit recorded in PACKAGE.json. The whole module and collection are frozen source witnesses; only the complete `fs-id1170655163482` subtree is translated by this continuation. The collection's 82 entries are not a claim that this packet translates 82 modules.

The inherited `A10-notice.txt` describes the **Indonesian comparison predecessor**, not this Punjabi packet. It remains unchanged as provenance. Neither that comparison's completion status nor its model label is being applied to the current Punjabi translation. The canonical English CNXML, not Indonesian wording, controls every source formula, exercise, answer and figure.

`LICENSE.txt` preserves the inherited license designation. The applicable license is [Creative Commons Attribution-NonCommercial-ShareAlike 4.0 International](https://creativecommons.org/licenses/by-nc-sa/4.0/), subject to inherited component-specific notices. Its [legal code](https://creativecommons.org/licenses/by-nc-sa/4.0/legalcode.en) sets the terms. Original contributing authors and publisher are credited in the reader. Original marks do not imply endorsement.

The three `CNX_ElemAlg_Figure_01_02_014*_new.jpg` files were fetched from the official repository at the exact source commit. Their URLs, byte counts, decoded dimensions and hashes appear in `source/SELECTION.json`. No figure was mirrored, resampled, redrawn or substituted.

`translation.json` is the new source-bound Punjabi translation. `guidance.html` is separately credited original guidance. `renderer_base.py` is an unchanged inspected source-bound rendering component from the admitted predecessor; `build.py` selects only its required definitions and does not execute its obsolete entrypoint. Rebuild offline with `python build.py`; run `python verify.py` after actual browser verification. The acquisition helper `study.py` is not required for an offline rebuild and may make three explicitly pinned source-image requests only when those files are absent.

Language evidence is documented separately in `LANGUAGE_NOTES.md`; no prose essay or dictionary entry certifies Pakistani classroom terminology.
