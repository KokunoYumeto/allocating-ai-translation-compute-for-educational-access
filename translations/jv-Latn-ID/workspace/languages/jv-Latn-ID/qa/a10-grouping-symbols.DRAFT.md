# A10 grouping-symbol draft — 2026-08-31

Status: source-keyed provisional academic Javanese and conversational ngoko
edits only. No generated CNXML, reader, narration, SSML, synthesis, visual,
screen-reader, native-educator, or listening pass is claimed by this handoff.
The whole A00/A10/AX-2 assignment remains unfinished.

## Exact contiguous boundary

Select module `m82453`, section `fs-id1170655150800`, direct element children
`[23:28]` (zero-based, end-exclusive), with the original section title reused as
context. This is all of the grouping-symbol introduction, not a hand-selected
set of examples. Both the Indonesian and English selected source elements were
read in full, together with the next paragraph to establish the boundary.

| Child | Source ID | Included content |
| --- | --- | --- |
| 23 | `fs-id1170654957487` | Grouping and punctuation analogy; three types introduced |
| 24 | `fs-id1170655219760` | Full note and all three bracket-name rows; nested equation `fs-id1167269979347` |
| 25 | `fs-id1170654880176` | Example introduction and `term-00011` |
| 26 | `fs-id1170654936322` | All three complete grouped example expressions |
| 27 | `fs-id1166424830424` | Spacing-only paragraph containing a newline |

The next excluded child is **28, `fs-id1170654957085`**, beginning the distinct
phrase/expression/equation lesson. Its examples and exercises belong to that
next lesson, not to the grouping introduction. No exercise, solution, or answer
is present inside the selected grouping span, and none is cut in half.

There are **8 source IDs total**, including shared section wrapper
`fs-id1170655150800`: **7 new IDs**, 2 MathML expressions, 0 CNXML tables,
0 media, 0 links, 0 exercises, and 0 solutions. Both MathML expressions contain
an `mtable`; these are layout tables, not two ordinary CNXML data tables.
The two source layouts present three bracket types and three algebraic examples,
not six separate top-level MathML expressions.

Ordered source IDs:

```text
fs-id1170655150800
fs-id1170654957487
fs-id1170655219760
fs-id1167269979347
fs-id1170654880176
term-00011
fs-id1170654936322
fs-id1166424830424
```

## Source witnesses

The retained Indonesian module is
`downloads/jv-Latn-ID/a10-source/translated/modules/m82453/index.cnxml`, SHA-256
`2c0b688d569044b128d589579e9ba7d871a0fb9ac7a670ac6f22d0ef2b66e635`.
The retained English module is
`downloads/jv-Latn-ID/a10-source/authority/source/modules/m82453/index.cnxml`,
SHA-256 `a8fdd235aa254c5a0a1248fa858e9b3579d9b40982bbc514cbe6a18c3e6fcbed`.
These are the same pinned A10 release/canonical witnesses used by the preceding
units; no new source acquisition or general audit was performed.

The English original names English punctuation; the Indonesian witness already
localizes that analogy to Indonesian. Both Javanese registers retain **basa
Indonesia** here so the source-reference claim stays accurate. This does not
relabel the Indonesian witness as Javanese or add a new claim about Javanese
grammar. The later grammar analogy needs a separate linguistic decision.

## Canon consultation and phrase decisions

At source-reading/drafting, fully reread the actual local readable C14 `owah`,
C16 `saka`, C21 `ping`, and C22 `para` entries. C14 supports the change verb used
in the conversational simplification sentence; C16 supports separation/source
`saka`; C21 and C22 ground the future multiplication/division terminology.
None of those entries alone attests a complete grouping-symbol term or a
mathematical simplification convention.

The new topic warranted read-only official KBJI checks. The complete retrieved
[kurung entry](https://kbji.kemendikdasmen.go.id/kata/kurung) distinguishes its
written-sign sense from physical enclosure; that supports retaining `kurung`
for the bracket labels. A readable indexed
[KBJI lampus result](https://kbji.kemendikdasmen.go.id/kata/lampus) includes
`lumpuk` in the gathering/collecting sense. A direct fetch of that page timed
out, so only the returned indexed entry was consulted, not a claimed full-page
read. Direct `lumpuk`/`waca` retrievals failed, and `prasaja`/`tandha` retrievals
also failed; none is counted as a successful new acquisition. The local shelf
was observed at **28 records** during final verification after the coordinator's
separate topic additions; this two-file task changed none of them. These web
consultations are not silently counted as additional pinned local records.

At revision/QA, fully read the newly available C26 `wolu` and C27 `sanga`
readable entries. They directly support the eight/nine components in future
speech for these examples; `wolu` and `sanga` do not by themselves establish
nested-grouping prosody. No numeral or formula was rewritten in this text draft.

| Exact source key or role | Draft decision and limits |
| --- | --- |
| Full introduction, `Simbol pengelompokan...` | Academic uses `simbol`, `ekspresi`, `njlentrehake`, and impersonal `bakal ditepungake`; ngoko uses `tandha`, `wujud aljabar`, `nggawe cetha`, and invitational `ayo kenal`. The punctuation analogy, separation/grouping meaning, and three-type count are retained. `titik loro` names the colon; `tandha wacan` is an existing-use-style compound, not newly proved by this shelf. |
| `Simbol Pengelompokan` | `Simbol Panglumpukan` / `Tandha Panglumpukan`. `panglumpukan` is a provisional nominal extension from grouping/collecting vocabulary, not a quoted KBJI mathematical headword. Educator review must decide whether this is preferable to an explicit `tandha kanggo nglumpukake`. |
| `Tanda kurung biasa` | `Tandha kurung biasa` / `Kurung biasa`. `kurung` has direct writing-sign evidence; `biasa` is retained as the source's differentiator. Parenthesis glyphs stay untouched. |
| `Tanda kurung siku` | `Tandha kurung siku` / `Kurung siku`. Retain the Indonesian-derived school label as a declared provisional loan rather than invent a supposedly standardized Javanese name. Square-bracket glyphs stay untouched. |
| `Tanda kurung kurawal` | `Tandha kurung kurawal` / `Kurung kurawal`. `kurawal` is likewise a declared unverified school-term loan. Curly-brace glyphs stay untouched; these braces are grouping signs, not an assertion of set notation. |
| `Berikut beberapa contoh ekspresi yang memuat` | `Ing ngisor iki ana sawetara tuladha ekspresi sing ngemot` / `Iki sawetara tuladha wujud aljabar sing nganggo`. Keep the following term element separate and preserve its source ID. |
| `simbol pengelompokan` | Lowercase counterpart of the same provisional heading term; no inconsistent alternate term introduced. |
| `. Nanti dalam subbab ini kita akan menyederhanakan ekspresi seperti ini.` | Academic uses the descriptive `bakal digawe luwih prasaja`; ngoko uses `ayo ngowahi ... dadi luwih prasaja`. This states the later simplification promise, not a new instruction to evaluate the displayed examples now. `prasaja` remains an unverified register choice here; no successful KBJI lookup is claimed. |

The context title `Gunakan Variabel dan Simbol Aljabar` is inherited from the
existing exact shared phrase row: `Nggunakake Variabel lan Simbol Aljabar` /
`Ayo Nganggo Variabel lan Tandha Aljabar`. It is not overridden by this file.
Eight new exact trimmed source-string keys are supplied. There are no new
unchanged-letter exceptions: this span's mathematical examples contain numbers
and operators, not variable letters.

## Mathematics and AX-2 integration requirements

Visible expressions must remain exactly:

```text
8(14−8)
21−3[2+4(9−8)]
24÷{13−2[1(6−5)+4]}
```

Do not evaluate them, reorder terms, replace the U+2212 minus or U+00F7 division
sign, or insert visible multiplication operators. Adjacent number/bracket
groups imply multiplication here. A spoken clarification may say that only in
an explicit source-bound rule, without rewriting source mathematics.

Only the three bracket-name `mtext` nodes change between ID and JV. Preserve
every other MathML text token, namespace, child order, empty node, attribute,
and operator. In particular:

- The bracket-name layout has three `mtr` rows with four `mtd` cells each; its
  two empty middle cells per row and the empty `mrow` inside each bracket pair
  remain. Left-alignment and the two `mspace width="0.15em"` nodes remain.
- The examples layout has one row with seven cells, including four empty
  separator cells. The three occupied cells retain `columnalign="center"`.
  Delimiter `stretchy="false"` attributes are not normalized or removed.
- Future narration must distinguish opening/closing parentheses, square
  brackets, and braces, and preserve the full nesting in all three examples.
  Existing operation/equality fixtures do not authorize flattening these layouts.
  Register two exact top-level-anchor+ordinal fixtures with language-specific
  prose handling and fail closed on altered delimiters, nested order, empty
  cells, or attributes.
- The source spacing node `fs-id1166424830424` must stay in CNXML/reader but have
  an explicit nonspoken-source entry; do not create an empty SSML paragraph.

There is **no bitmap/vector asset blocker**: zero media references occur here.
The integration blockers are registering the contiguous unit descriptor,
source-bound narration/layout fixtures, the new nonspoken spacing anchor, and
regression tests. New files in this handoff do not modify shared build code,
prior outputs, source/canon locks, coverage, or existing receipts.

## Verification checkpoint

The intended verification is in-memory replay using the existing `select`,
`translated`, and `validate` functions, with new keys merged into the existing
shared phrases and no writer invoked. The handoff records the actual result
below after that replay; draft prose alone is not a build pass.

**Actual replay: PASS, 2026-08-31.** Both full source-module hashes match the
source lock. All 8 new exact trimmed keys occur in this span, the shared title
resolves without conflict, and both Javanese tracks pass existing ID/structure,
in-unit-reference, MathML attribute/non-prose-token, and numeric-fact validation.
An independent check confirms both ID and EN ordered ID lists, the two layout
cell-count patterns `[[4,4,4],[7]]`, the absence of media/exercises/solutions/links,
and all three exact expressions printed above. Replaying both translations
produces identical bytes in memory. Deliberate operator and cell-alignment
mutations are rejected by validation. No generated output was written.

Current edits-file SHA-256:
`6318da4f3b856b80940aad157bbe38788dfd81c04edf814733bb999c9f0e7c47`.

For later integration, the in-memory XML hashes using `draft_units` serialization
(`ET.tostring(..., encoding='utf-8', xml_declaration=True) + b'\n'`) are:

| Register | Expected SHA-256; witness only, not a generated file claim |
| --- | --- |
| `jv-academic` | `3886f30a33cf6c0f2bb194f92bea0fcf80a96604985ca896e7db735eca1f0eea` |
| `jv-conversation` | `0b73a1a364196a02cb1804df5375fa6b120407d711dbd70cd67dfebc14555790` |

## Production integration — 2026-08-31

The exact `[23:28]` slice is now registered and built into three CNXML tracks,
one offline reader, three transcripts and three SSML files. All 19 grouping
tests pass with no skipped production tests. Two whole-source MathML fixtures
preserve every delimiter, nesting level, empty layout cell and all five
implied multiplication sites. Trailing layout-only fs-id1166424830424 remains
in CNXML/HTML and is explicitly omitted from narration/empty SSML marks.

Main acquired and fully read official kurung/tutup as C29–C30; earlier online-
only acquisition descriptions are historical. Component evidence supports the
bracket/open-close naming convention, not universal mathematical terminology
or listening approval. Current build receipts bind exact production hashes.
Continue expression/equation `[28:40]`; the module and full assignment are not done.
