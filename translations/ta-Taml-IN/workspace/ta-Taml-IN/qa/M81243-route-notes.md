# m81243 recovery route hub — source-level draft

2026-08-31. Bounded ownership: only `translation/recovery-m81243-route.xhtml` and this note. Version 0.1.0. No existing source/companion, shared log, builder, stylesheet, reader, EPUB, PDF or commit was changed. This closes an authored-route draft, not the complete delivery or full assignment.

Current route fragment SHA-256: **`a2fc64c525af6c3f21630075aa0c4fcac4a08dda209a54cdf7dad2746f1f7618`**.

## Scope and integration condition

The fragment contains **9 top-level sections, 43 unique `ta-route-*` IDs, 12 new assessed items and 12 one-to-one answers**: six final items F1–F6 and six retries T1–T6. Each has a reason, misconception feedback, and the same-objective numbered recovery route. The six objective labels exactly match the current front-matter list, in canonical order; the English and Indonesian metadata lists were also read directly.

There are **84 hrefs**: 54 within this hub and 30 pointing to IDs in the four actual existing companions. All resolve in the intended union, with no duplicate IDs across these five fragments. There is no network URL, file-path link, downloaded resource, image, script or form. This does **not** mean the fragment works as an isolated file: it must be bundled with those four companion fragments into one reader, retaining the actual IDs. No such workflow build was performed here. Parent owns bundling, a table-of-contents entry/return navigation, rendered testing and the final closure check. Do not present a source-only reader or this unbundled fragment as the completed recovery workflow.

The hub explicitly requires a notebook record of each local M1–M4 **complete attempt**, with all requested subanswers and reasoning correct. It never combines isolated passes from different attempts. It uses four checkpoints: U001, U002, the joined U003–U005 companion, and U006. Even where a standalone companion allows its own T set to finish locally, this integrated route asks for its full M set again before moving on. This is an explicit new routing rule, not an edit of that companion.

The source's self-confidence/teacher-facing checklist is left unchanged. A learner may reflect on confidence but cannot use “yes, I can” as the route's pass condition; full answers and reasons recorded on paper are the condition. There is no required teacher approval, online grading or external service. A later learner can pause by writing the next item number and resume without an automatic reminder.

After the four local checkpoints, one full F1–F6 attempt is checked. Any miss goes through its numbered hub recovery route and a matched T practice item. Then the learner attempts the full T1–T6 set without viewing answers. A failed full retry returns to remediation, then a new whole F or T attempt; scores cannot be combined across sets. Completion requires all six requested answers/reasons in one full attempt and all four prior checkpoints. The text states that repeated questions do not establish broad transfer or validated mastery.

The source U001 standalone handoff still says its next Tamil unit is not ready. The hub does not mutate that historical file; it explicitly tells readers to use the four-row integrated route rather than older standalone release notes. Parent integration should keep this distinction visible.

## Six-objective assessment mapping

| Source objective / subsection | Final item and checked answer | Retry item and checked answer | Hub remedy and real explanation |
|---|---|---|---|
| 1. Counting/whole numbers, `fs-id1830385` | F1: from 0,3,8, counting 3,8; whole 0,3,8; explain 0 and overlap | T1: from 0,2,6, counting 2,6; whole 0,2,6 | `ta-route-R1` → `ta-R2` |
| 2. Model whole numbers, `fs-id2340048` | F2: 241 → 2 hundreds squares,4 tens rods,1 ones block; 200+40+1 | T2: 305 → 3,0,5; 300+0+5; why zero placeholder remains | `ta-route-R2` → `ta2-R2`; T2 feedback also `ta2-R3` |
| 3. Place value, `fs-id1883656` | F3: 7 in40,706,052 is hundred-thousands, in706 thousands group, value700,000 | T3: 3 in8,304,071 is hundred-thousands, in304 thousands group, value300,000 | `ta-route-R3` → `ta-large-R1`, `ta-large-R2` |
| 4. Name whole numbers, `fs-id1321580` | F4:12\|008\|045 → பன்னிரண்டு மில்லியன், எட்டு ஆயிரம், நாற்பத்து ஐந்து | T4:7\|030\|006 → ஏழு மில்லியன், முப்பது ஆயிரம், ஆறு | `ta-route-R4` → `ta-large-R4` |
| 5. Write whole numbers, `fs-id1339359` | F5: நான்கு மில்லியன், எழுபது ஆயிரம், ஒன்பது →4\|070\|009 →4,070,009 | T5: ஒன்பது மில்லியன், ஐந்து ஆயிரம், இருபது →9\|005\|020 →9,005,020 | `ta-route-R5` → `ta-large-R3`, `ta-large-R4` |
| 6. Round whole numbers, `fs-id2472737` | F6:49,650 nearest1000→50,000; distances650/350; carry9 into4 | T6:999,500 nearest1000→1,000,000; distances500/500; half-up tie and new leading1 | `ta-route-R6` → `ta-round-R1` through `ta-round-R4` |

F2/T2 specify fewer than ten tens/ones so the expected base-ten decomposition is unambiguous. Drawings with correct group counts/values and the equivalent written model are accepted; artistic quality is not scored. F4/T4 allow clear equivalent Tamil phrasing such as எட்டு ஆயிரம்/எட்டாயிரம் and முப்பது ஆயிரம்/முப்பதாயிரம், while retaining exact group values. Standard international comma grouping is explicitly required for F5/T5 rather than inferred as a board-wide norm.

## Actual canon and source consultation

At drafting read actual existing OCR pages 7,11,20,35, plus the already-read rounding pages30/31 from the immediately preceding bounded task. Visually inspected full pages11 and35 for this hub to resolve corrupt OCR digits and English-letter substitutes. Page7 supplies **கற்றல் நோக்கங்கள்**; page11 supplies **இடமதிப்பு விளக்கம்**, grouped reading and **எண்ணின் பெயர்**; page20 supplies **தீர்வு**, stepwise place-value comparison; page35 explicitly starts **இயல் எண்கள்** at1 and **முழு எண்கள்** at0. Pages30/31 support exact versus approximate values and the immediately-right-digit/compare-with5/carry/zeros procedure. Canon's Indian grouping was not substituted for the source's international groups, and no canon exercise was copied.

During revision/QA reread the actual page11 and31 OCR alongside the group/zero wording and rounding answer, using the already visually resolved page values rather than corrupt OCR. Read the existing large-number R4 explanation and its M3/M4 answers directly to coordinate spaced versus compounded Tamil number names and leading/interior-zero language. Read actual local M/retry gate paragraphs and section IDs in all four companion files. A long combined output was truncated; U002 and large-number gate paragraphs and titles were then retrieved separately. Do not infer approval from this consultation.

Parent independently reviewed the hub opening and identified an overly broad scope sentence. It was corrected: this route does not teach **calculations** with negative numbers, fractions or decimals, but such values can appear in source whole/nonwhole classification questions and their support. This avoids falsely claiming those examples are absent from an eventual complete integrated source reader. No question, answer or link changed in that revision.

Shared canon/terminology logs remain parent-owned; this note holds the bounded consultation record. Tamil-native/educator review of cadence, instructional density, model terminology and composed number names is still pending. No native-speaker approval, grade placement validity, board alignment or efficacy is claimed.

## Input identities

| Input | SHA-256 |
|---|---|
| `translation/m81243-frontmatter.cnxml` | `190a493d146d8e943038c9c507cdb2d04042f85886ec7ebe06a22c06e22473b0` |
| `translation/recovery.xhtml` | `4116b42e061de2e8a5693e62fdb7ee36d8f8615ab922d485288df4b4e4e92f45` |
| `translation/recovery-u002.xhtml` | `b61edfe7fe1084267b322ae7d9c5bc10694f069e0557cf2f9906fe7b552da463` |
| `translation/recovery-large-numbers.xhtml` | `37f05038e4c238ca0e7a0951f2b136e84aa7c6391a6acac7da7678498361baf8` |
| `translation/recovery-rounding.xhtml` | `84fe718d7fd236bbe943753b4bedad5ad1041856b15343498e653c1de8132ab3` |
| English witness `provenance/m81243.en.cnxml` | `396b0029798e054e5db6d7acde738cec9f9d8b86bc81da8cc3690d01ec07cf2b` |
| Indonesian witness `provenance/m81243.id-ID.cnxml` | `7153ce88bcd4aea07fab4075bdb025884e07d534aafb6d06ff1bfbedbc46f251` |
| Actual OCR `page-007.txt` | `bcfa25c16296d82b8c9b6d8c15e6d982225b966387ff45cc99f1fe20f2af242f` |
| Actual OCR `page-011.txt` | `e2b1d6e39cf5dec4d0cf3f17575c9f47c90c3620c03620c965f8bec616faf26a` |
| Actual OCR `page-020.txt` | `c76fc7d9b9c95a59e0c485f23a948bb1291452a2b67926478fbb42ec74f0f0c3` |
| Actual OCR `page-035.txt` | `af5415d3885e7bd4aa096edfab7a9073d39151c048a38b204c2d798987f0cc6b` |
| Actual image `page-011.png` | `16be1531fa330c64afa212f2abb1c719cd28a9d17d762dbb7dbf4e9805065d43` |
| Actual image `page-035.png` | `06523e1d96181492eddd2b8f0ebb88fa385d8138689e83f78d48f08e046cfe16` |

Rounding page30/31 OCR and image hashes are recorded in `ROUNDING_COMPANION_NOTES.md`; those actual files were used, not an unverified paraphrase. English pin `38cae454e644abf9f0a623e876994553881597c9`; Indonesian v0.2.7 / commit `3de9207f56f8b5c57c017abf973fb04e00d740f1`.

## Completed QA and remaining work

Passed ElementTree/lxml parsing; unique IDs; exact six-objective labels and data mappings; 12 one-to-one answers; every reason/feedback/remedy; all intended-union references; no active/network content; and the four-row semantic checkpoint table's column/row header scopes. All **six MathML expressions** passed: four exact decompositions and two rounded values. Checked both classifications, both model decompositions, both digit positions/group names/contributions, four number-name/group compositions, zero padding, both distances and both carry chains independently. The final/retry question sets are parallel samples, not a psychometrically validated instrument.

Manually traced: no prior checkpoint; all local checkpoints passed; one or multiple F misses; partial T practice; one full T pass; one full T failure; full-set repeat; and pause/resume on paper. A full T pass cannot replace a missing local checkpoint. Browser/EPUB/PDF navigation, actual back-control behavior, layout, screen reader behavior, integrated local asset closure and native/learner testing remain pending. No rendered-reader pass is asserted from XML checks.

### Rerunnable read-only check

Run this Python block from the workspace root; no third script file is needed. Set `PYTHONIOENCODING=utf-8` when using a PowerShell here-string and `python -`.

```python
from pathlib import Path
from hashlib import sha256
from collections import Counter
from lxml import etree
import xml.etree.ElementTree as E
X = '{http://www.w3.org/1999/xhtml}'
M = '{http://www.w3.org/1998/Math/MathML}'
C = '{http://cnx.rice.edu/cnxml}'
p = Path('ta-Taml-IN/translation/recovery-m81243-route.xhtml')
raw = p.read_bytes()
r = E.fromstring(raw)
etree.fromstring(raw)
ids = [e.get('id') for e in r.iter() if e.get('id')]
assert len(ids) == len(set(ids)) == 43
assert all(i.startswith('ta-route-') for i in ids)
owners = {i:p.name for i in ids}
for name in ['recovery.xhtml', 'recovery-u002.xhtml', 'recovery-large-numbers.xhtml', 'recovery-rounding.xhtml']:
    for e in E.parse(p.parent/name).getroot().iter():
        if e.get('id'):
            assert e.get('id') not in owners
            owners[e.get('id')] = name
links = [e.get('href') for e in r.iter(X+'a')]
assert len(links) == 84
assert all(h.startswith('#') and h[1:] in owners for h in links)
q = [e for e in r.iter() if e.get('data-kind')]
a = [e for e in r.iter() if e.get('data-answer-for')]
assert Counter(e.get('data-kind') for e in q) == dict(final=6, retry=6)
assert Counter(e.get('data-objective') for e in q) == {str(i):2 for i in range(1,7)}
assert Counter(e.get('data-answer-for') for e in a) == Counter(e.get('id') for e in q)
for item in q:
    answer = next(e for e in a if e.get('data-answer-for') == item.get('id'))
    assert len(answer.findall(X+"p[@class='reason']")) == 1
    assert len(answer.findall(X+"p[@class='feedback']")) == 1
    assert item.get('data-remediation') == 'ta-route-R'+item.get('data-objective')
    assert '#'+item.get('data-remediation') in [e.get('href') for e in answer.iter(X+'a')]
    assert '#'+answer.get('id') in [e.get('href') for e in item.iter(X+'a')]
for e in r.iter():
    assert e.tag.startswith((X,M))
    assert e.tag.rsplit('}',1)[-1] not in {'script','iframe','img','object','embed'}
    assert not any(k.lower().startswith('on') for k in e.attrib)
for tr in r.findall('.//'+X+'table/'+X+'thead/'+X+'tr'):
    assert all(c.tag == X+'th' and c.get('scope') == 'col' for c in tr)
for tr in r.findall('.//'+X+'table/'+X+'tbody/'+X+'tr'):
    assert len(tr) == 3 and tr[0].get('scope') == 'row'
math = list(r.iter(M+'math'))
assert len(math) == 6
for m in math:
    s = ''.join(m.itertext()).replace(',','')
    if '≈' in s:
        n, result = map(int,s.split('≈'))
        lo = n//1000*1000
        hi = lo+1000
        assert result == (lo if n-lo < hi-n else hi)
    else:
        left,right = s.split('=')
        assert sum(map(int,left.split('+'))) == int(right)
for values, natural, whole in [([0,3,8],[3,8],[0,3,8]), ([0,2,6],[2,6],[0,2,6])]:
    assert [n for n in values if n >= 1] == natural
    assert [n for n in values if n >= 0] == whole
assert 2*100+4*10+1 == 241 and 3*100+0*10+5 == 305
assert (40706052//100000)%10 == 7 and 7*100000 == 700000
assert (8304071//100000)%10 == 3 and 3*100000 == 300000
assert (40706052//1000)%1000 == 706 and (8304071//1000)%1000 == 304
words = {'பன்னிரண்டு':12,'எட்டு':8,'நாற்பத்து ஐந்து':45,'ஏழு':7,'முப்பது':30,'ஆறு':6,
         'நான்கு':4,'எழுபது':70,'ஒன்பது':9,'ஐந்து':5,'இருபது':20}
cases = [(['பன்னிரண்டு','எட்டு','நாற்பத்து ஐந்து'], '12,008,045'),
         (['ஏழு','முப்பது','ஆறு'], '7,030,006'),
         (['நான்கு','எழுபது','ஒன்பது'], '4,070,009'),
         (['ஒன்பது','ஐந்து','இருபது'], '9,005,020')]
for parts,written in cases:
    values = [words[w] for w in parts]
    assert values == [int(x) for x in written.split(',')]
    assert sum(v*s for v,s in zip(values,[1000000,1000,1])) == int(written.replace(',',''))
    assert written == format(int(written.replace(',','')), ',')
for n,distances in [(49650,(650,350)), (999500,(500,500))]:
    lo = n//1000*1000
    assert (n-lo,lo+1000-n) == distances
front = E.parse(p.parent/'m81243-frontmatter.cnxml').getroot()
objectives = [''.join(e.itertext()) for e in front.findall('.//'+C+'list/'+C+'item')]
labels = r.find(X+"section[@id='ta-route-objectives']/"+X+'ol')
assert [''.join(e.itertext()).split(' — ')[0] for e in labels] == objectives
print('PASS: 43 IDs; 84 intended-union links; final6/retry6; 12 answers; six objective pairs; six MathML')
print('SHA-256:',sha256(raw).hexdigest(),'bytes:',len(raw))
```
