# M81244 U009 - Use Addition Notation

Tamil AI draft and source-level review: 2026-08-31. Only `translation/m81244-fs-id2601285.cnxml` and this receipt are owned in this task. This is a source-faithful instructional fragment, not a new companion or a claim that the module/educational route is complete. No source witness, shared terminology/log, builder or earlier translation is changed; no build or commit is run.

## Actual source boundary

Read the entire `m81244#fs-id2601285` section in both actual witnesses:

- English: `downloads/osbooks-prealgebra-bundle-38cae454e644abf9f0a623e876994553881597c9/modules/m81244/index.cnxml`, whole-module SHA256 `b32058ce714e5fd43b010ccc81b2ae00a11567b229e9f96dda7b85b3cd82ba6b`.
- Indonesian: `downloads/openstax-prealgebra-2e-id-ID/modules/m81244/index.cnxml`, whole-module SHA256 `d23343f302c34436169e88ffdbc4eab37baabf0a3d1134755b2f5676c75e1cc6`.

The pinned repositories are unchanged. The exact first instructional section has **143 elements, 30 ordered IDs, 19 MathML expressions, 3 exercises, 3 solutions and 0 media**. There is one true five-column table with a header row and one body row. All six exercise subparts request **math notation to words**, not evaluation of sums.

The final source note is `fs-id2216255`; its exercise is `fs-id2334163`, problem `fs-id1525211`, solution `fs-id1577525`, and last named descendant/list **`eip-id1168288338262`**. Next sibling **`m81244#fs-id2145437`**, Model Addition of Whole Numbers, is outside scope. The module title, metadata/frontmatter, two preceding readiness notes `fs-id2299412` and `fs-id1122444`, and all later sections remain separately pending.

The generic source term ID `term-00001` remains unchanged. Future multi-module reader assembly must respect module-scoped identifiers; this task does not rename source IDs to avoid cross-module collisions.

## Actual canon consultation and register choices

Before drafting, read actual OCR pages5,36,175 and searched the already-OCRed corpus for addition/sum/expression/addend anchors. Also read existing p37-38 passages for focused context rather than acquiring or OCRing new pages. The PDF skill was used read-only to inspect complete existing page36 and175 images: p36 resolves OCR `கூருதல்` to **கூடுதல்**, and p175 resolves the bilingual glossary's layout and corrupted English OCR.

- **கூட்டல்** - addition, directly used in actual p36 / printed30. **செயல்பாடு** - operation, supported by p36's arithmetic-operations wording and p175's operation-on-variables entry. Exact elementary compound choices remain contextual rather than official terminology mandates.
- **கூடுதல்** - sum, supported by p36's actual sentence about the sum of two numbers. This is kept distinct from **கூட்டல்**, the operation. The plain noun **மொத்தம்**, also visible on p36, is used for total hours. **கூட்டுத்தொகை** is a plausible alternative, but not falsely attributed to these consulted passages; it is not silently mixed into this fragment.
- **குறியீடு** - notation, explicitly matched to “Notation” in actual p175. The initial considered expansion **குறியீட்டு முறை** is unnecessary here; use the directly attested noun in the title/table/prose. **கூட்டல் குறி** describes the plus sign.
- **கோவை** - expression. Actual p175 attests **இயற்கணித கோவைகள்** for “Algebraic Expressions” and separately **இயற்கணித கூற்று** for “Algebraic Statement”. Applying the singular base **கோவை** to this arithmetic expression is a transparent, provisional extension, not evidence that this exact elementary definition is quoted in the canon.
- **கூட்டப்படும் எண்கள்** - addends. No exact addend headword was found in the consulted readable corpus. This descriptive phrase preserves the source definition and does not pretend to be an attested technical headword. **கூட்டல் உறுப்புகள்** is another possible register; not selected because it would introduce another unexplained technical noun at this point.
- **வாசிக்கும் முறை** - table “Read as”; a plain contextual label. Read plus as **கூட்டல்**, e.g. “ஏழு கூட்டல் ஒன்று”, and describe the result as “ஏழு மற்றும் ஒன்று ஆகியவற்றின் கூடுதல்”. These give both original phrasings without replacing them by a calculated numeral.
- Continue established number names, including separated **இருபத்து ஒன்று**, **பதினாறு**, **நூறு**, **இருநூறு**. Source “one hundred” is rendered idiomatically as நூறு while preserving100; it does not require the redundant literal ஒரு நூறு.

The 2018 SCERT reference supports this draft's register; it does not establish current syllabus alignment, native-speaker approval or independent linguistic certification. No new download, OCR output or PDF was generated.

## Source/translation decisions and limits

- Preserve the college student's part-time employment and Monday3hours/Friday4hours context; do not localize the person, dates or units. Reorder the last Tamil clause so the unchanged MathML period after4 remains at the sentence end.
- Preserve every Arabic digit token in prose, including explanatory addends7,1 and12,14; preserve all19MathML trees and punctuation unchanged. The final answers remain word readings and sum phrases, not8/26/12/29/37/300. Calculated totals may be useful later in a separate companion but are not inserted here.
- Retain both empty label nodes, table column specifications, alignments, six list IDs/types, circled tokens, inline emphasis, term/equation/example/note hierarchy and all source IDs. The source wraps3+4 in an `equation` element despite giving no equality; preserve that structural wrapper without labeling the mathematical expression an equality.
- The table's Tamil alternative describes its actual two rows/five columns and all cell contents. No structural accessibility correction is needed. English aria text uses plural “Expressions” whereas the actual header is singular “Expression”; Tamil consistently uses கோவை. It does not add the numerical total7.
- The source's broad sentence calling a numbers-and-operations “math statement” an expression is translated faithfully. This can blur the expression/equation distinction if read as a formal universal definition; the next section introduces equations separately. Do not silently insert a new exclusion or new example into this source fragment. A companion can later make the distinction explicit.
- Only the bounded section is delivered. Rendered table layout, full module frontmatter, readiness questions, later-source translation, teacher-independent recovery routing and native/accessibility review remain outside this checkpoint.

## Completed source and word-pair QA

Reopened the actual authored fragment, both complete source sections and the selected actual canon passages during revision/QA. P36's arithmetic-operation/sum/total sentences were compared with the selected wording; p175's expression/statement/notation entries were reconsulted through their OCR locators, retaining the spelling resolved from the previously inspected complete page images. The existing p11 number-name example directly supports separated **இருபத்து ஒன்று**. No further corpus acquisition was needed. The broad source definition of expression remains a declared source-level limitation, not a silently repaired definition.

The following read-only checks passed on 2026-08-31:

- Parsed XML and recursively compared element hierarchy/order and all stable attributes against **both** witnesses. The only permitted attribute differences are the root Tamil `xml:lang` and translated table `aria-label`. Exact counts are 143 elements,30 ordered/unique IDs,19 MathML roots,3 exercises,3 solutions,0 media. Retained two empty labels, five colspecs, ten table cells, six lists, twelve circled-token spans and five emphasis nodes.
- Compared every MathML tree, attribute, token and punctuation with each witness, including the period following4 in the opening paragraph. Compared the entire body Arabic-digit sequence in order, including the four prose addend tokens7,1,12,14. Both comparisons pass without numerical normalization or operand reordering.
- Decoded the twelve Tamil solution phrases independently to numbers and compared the **ordered operand pairs** to their actual question MathML. Each part has both a plus-reading and a sum-description. Separately decoded all twelve English and all twelve Indonesian source solution phrases to those same operands; all agree. The two introductory/table readings of3+4 also decode to3,4. No solution was changed to an evaluated total.
- Confirmed the actual two-row/five-column table has five entries in each row and that all five Tamil headers appear in its accessibility description. There are no media or external/reference targets within this fragment, so no new asset task is needed.
- Verified the closing note/solution/list boundary directly, and verified that both source sections have next sibling `fs-id2145437`. No later node is present in the Tamil fragment.

Exact source problem/solution mappings checked:

| Exercise | Problem | Solution | Ordered pairs; both word forms retained |
|---|---|---|---|
| `fs-id2294573` | `fs-id1330068` | `fs-id2294519` | (7,1); (12,14) |
| `fs-id1386428` | `fs-id2842881` | `fs-id1948344` | (8,4); (18,11) |
| `fs-id2334163` | `fs-id1525211` | `fs-id1577525` | (21,16); (100,200) |

Final Tamil fragment: **9,108 bytes**, SHA256 **`83b547490aab15c693225a832c9d03e14f2bf1ac8d4cde105470a6e2c601b313`**. Both whole-witness hashes still match those recorded above. Storage remained above13GB free throughout final checks, well above the100MB write-stop threshold. Only the two owned files were authored; existing unrelated worktree changes were left untouched.

These are author-level semantic review and reproducible machine checks, not an independent human/native-speaker review, a rendered-layout audit or a claim of module completion. The precise continuation is **m81244#fs-id2145437**; the separate frontmatter/readiness work and later module sections are still pending.
