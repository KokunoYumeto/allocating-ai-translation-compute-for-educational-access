# M81245 U019 figure inventory notes

Status: the stable first instructional section `m81245#fs-id1799687` contains no media. No `assets/u019/` directory or SVG was created. This is a bounded inventory checkpoint, not a rendered-unit, module-completion or assignment-completion claim.

## Stable boundary and witnesses

The final Tamil fragment is `translation/m81245-fs-id1799687.cnxml`, 8,530 bytes, SHA-256 `04f58da6e1560160273afcb18161dbfb8f5d955ef0e4f1eed577ef6393524883`. Its title is “கழித்தல் குறியீட்டைப் பயன்படுத்துதல்”. The translator's source and review record is `qa/M81245-U019-translation-notes.md`.

The pinned local witnesses were independently parsed at the exact section ID rather than inferred from the module summary:

| Witness | Complete file identity | Selected-section inventory |
|---|---|---|
| English | 95,095 bytes; SHA-256 `02df262b21ee15472223ae863bc5f3bcb3714389a8ef46e66c8b4469c6972841` | 133 elements; 28 unique IDs; 14 MathML roots; **0 media; 0 images; 0 alternatives** |
| Indonesian | 95,387 bytes; SHA-256 `d6829f9326dfcb9d30e589b2917bbc18ab2c871b0f44b939b1ceeb477ec8fe3f` | 133 elements; 28 unique IDs; 14 MathML roots; **0 media; 0 images; 0 alternatives** |
| Tamil | 8,530 bytes; SHA-256 `04f58da6e1560160273afcb18161dbfb8f5d955ef0e4f1eed577ef6393524883` | 133 elements; 28 unique IDs; 14 MathML roots; **0 media; 0 images; 0 alternatives** |

Both complete witnesses place `fs-id1824154` immediately after the selected section. That next section contains media according to the module inventory, but it is outside U019 and was not treated as an asset source. The stable U019 fragment ends at source ID `eip-id1168288179457`.

U019 includes a real two-row, five-column CNXML table in note `fs-id1718072`. It remains document structure, not a picture dependency. This figure lane does not replace it with an SVG or invent a media reference.

## Canon consultation

Before the inventory note was drafted, the current canon receipt and actual OCR for PDF pages 37 and 38 were read; the full page images were then opened to resolve the OCR's damaged operators. Page 37 visibly shows `7 − 3 = 4`, contrasts it with `3 − 7`, and repeatedly uses `கழித்தல்`. Page 38 visibly uses `வித்தியாசம்` for a subtraction result in its distributive-property explanation. These confirm that the stable Tamil section is about subtraction notation and not an omitted pictorial model. They do not create a source figure or authorize moving U020 media into U019.

At revision/QA, the saved Tamil fragment was reopened beside both actual selected source sections, and the visible page 37 subtraction example was checked again after its OCR. The three source selections agree on 133 elements, all 28 IDs, 14 MathML roots, zero media, zero images and the same final named descendant. No source-image or alternative-text discrepancy exists within this boundary because no media node exists.

## Asset decision and limits

There is nothing to redraw, localize, title, describe or render-check for U019. Creating even an empty asset placeholder, readiness image, decorative subtraction model, table screenshot or borrowed figure from `fs-id1824154` would fabricate source scope and was therefore avoided.

Consequently:

- no Tamil figure title/description is pending for this section;
- unique-SVG-ID, active-content, geometry and font-render checks are not applicable;
- no canonical raster was opened as if it belonged to U019;
- no existing asset, translation fragment, manifest, source gate, status file, terminology file or shared log was edited.

Future work on `m81245#fs-id1824154` requires an explicit new ownership handoff and a fresh media/pixel inventory.
