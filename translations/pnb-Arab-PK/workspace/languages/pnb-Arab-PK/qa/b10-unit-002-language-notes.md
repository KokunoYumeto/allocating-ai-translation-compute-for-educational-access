# B10-002 — complete first-section input translation

This is a complete scoped input translation, not a reader or full-book completion. The source scope is Chapter 1's complete earlier title/index/introduction followed by all of Section 1.1, Mathematical Statements, including its three subsections, objectives, reading questions and both active practice/additional-exercise includes. The next outside section is `sec_logic-implications.ptx`. Nothing earlier in Chapter 1 is silently skipped.

## Current checkpoint

All 559 source-bound text/data slots have been drafted and revised: the complete chapter opening, main section, practice and additional exercises, and six pinned cached static question versions. Actual canonical English and Indonesian comparison were read for all 466 authored slots. The remaining 93 cached static slots were read from the pinned cache, whose complete English blob is byte-identical in the Indonesian repository; no Indonesian static translation is claimed. Ten separately original notes supply terminology, source corrections and scope limits. The read-only input suite passes 6,798 checks and 30 detached mutations; two complete final runs returned identical output and are recorded in the scope plan. Production is not yet built. The full source inventory and replayable read-only inventory/input-QA programs are in `plans/B10-002-scope.json`.

The main witness retains the chapter opening and complete first section, expands only its two active XML includes, and adds a closing chapter tag to make the selected prefix parseable. This is not a claim that the whole chapter is present. The two local PG text includes remain inert, separately hash-bound dependencies. Source comments are retained but inactive. The second witness retains six selected pinned cached WeBWorK records; their fixed seeds and generated-source identity are not confused with authored PTX or a newly executed exercise service.

Counts: 559 planned text/data slots, 337 exact TeX owners (279 authored PTX plus 58 cached static), 17 authored XML IDs, 18 exercise-label anchors, 23 exercise nodes, seven authored tables plus one cached table (104 cells). The two cache list labels `a.` are numbering patterns, not IDs. The single source figure contains truth tables; there are no source image assets. All raw source macros and the two PG files remain unchanged. No upstream code, network grading, TeX, Perl or PG is executed.

## Actual canon reading and drafting decisions

Existing readable UTF-8 paragraphs were actually read: R1 line 28 (C01, ability/passive), line 39 (C02, obligation); R2 line 27 (C05/C06, purpose and alternatives); R3 line 31 (C09, reminders), line 25 (C10, qualifications), line 38 (C11, reasons). The draft receipt records exact paragraph/text/HTML hashes and new unit-specific applications. No canon snapshot was rewritten, downloaded or OCRed. These prose loci are register evidence, not specialist mathematical authority or native-reader certification; their historical/political assertions are not adopted.

- C01/C02 guide Punjabi `سکدے آں`, `چاہیدا اے` and direct learner instructions. Modal ability is not silently substituted for logical entailment.
- C05 keeps pedagogical purpose `تاں جے` distinct from implication `جے ... تاں`. Edith's sufficient cookie condition and Florence's necessary condition remain different.
- C06 and C10 support explicit contrasts and separately original qualifications rather than silent correction of source text.
- C09/C11 support `چیتے رکھو` and `کیوں جے` in explanatory prose. Mathematical correctness is checked from the actual source and mathematical reasoning, not inferred from the language canon.

Current provisional choices: statement `بیان`; argument `دلیل`; premise `مقدمہ`; conclusion `نتیجہ`; valid `منطقی طور اُتے درست`; sound `ٹھوس`; quantifier `کمّیتی نشان`. The full source definition of sound retains both validity and true premises; it must never collapse into merely a true conclusion. The separately original bridge makes these distinctions and English/Urdu access labels explicit. `ٹھوس` and `کمّیتی نشان` are provisional compositional choices, not asserted standardized Punjabi logic terminology.

The fictional trolls remain trolls, with source categories knight (سورما) and knave (دغا باز) explained by the author's always-truthful/always-lying conditions. The draft does not invent the investigation's answer. Source names Edith and Florence are transliterated as ایڈتھ and فلورنس. Cookie is rendered بسکٹ without changing the necessary/sufficient condition. All source numerals and TeX placeholders remain exact; prose numerals are isolated LTR.

The canonical truth tables use `T` and `F`; Indonesian replaces them with `B` and `S`, and its index replaces `iff` with `jhj`. The Punjabi translation retains the exact canonical data and abbreviation in LTR isolates. A separate original legend explains them; no truth-table value is silently localized or reordered. Intermediate checks caught a draft emphasis/quote order mismatch in the connective explanation, which was corrected while retaining the Punjabi sentence's meaning. The source's unproved/factually false atomic examples remain examples of statements, not falsely labeled true claims.

## Source issues retained with separately original corrections

1. Canonical subsection 3 paragraph 11 says every element of `x` belongs to `y` while explaining `A` subset of `B`; Indonesian changes those two TeX nodes to `A` and `B`. Preserve the canonical `x`/`y` source text and add a separately original correction explaining the intended sets. Do not import the Indonesian change into the canonical witness.
2. Practice exercise `rs-statements-quant-interpretation`, final choice, has the incomplete formula `\exists y \forall x`, lacking the predicate. Indonesian adds `P(x,y)`. Retain the raw formula and flag the omission separately; a new renderer must not invent the missing node silently.
3. Practice exercise `rs-statements-quant-neg` marks “Someone is never fooled” as the correct counterpart of “you cannot fool everyone at the same time.” The latter's time-first reading is `∀y∃x¬P(x,y)`, not `∃x∀y¬P(x,y)`. Two people taking turns being the unfooled person show the distinction. Preserve the source correct flag and wording, but visibly warn that the answer key does not match this scope. Do not offer unquestioned automated grading.
4. Cached `webwork-4`, seed 4, uses `16x+1` and correctly calls `P(11)` false, but its solution later claims `P(1)` true. Over the natural-number domain, `16·1+1=17` is odd; the final existential claim is a source error. The same erroneous even-coefficient branch is present in both inert PG files. Keep that raw source and add an explicitly original arithmetic correction.
5. Authored interactive/static pairs are not interchangeable numerical instances: PG quant1 selects an even coefficient while its adjacent static PTX uses 17; PG quant2 selects an odd coefficient while adjacent static PTX uses 18. The cached seeds use 16/11 and 9/13 respectively. Preserve the separate versions and their identity; do not swap source answers or claim a single matching generated instance.

6. The additional-exercise strict-between formula quantifies over equal endpoints as well. When `x=y`, no strict-between witness exists. The original note explains that two distinct real endpoints need an explicit `x≠y` condition and that distinctness alone does not make the statement true for integers. The canonical formula is unchanged; this is not a silent repair.

These are bounded content observations, not a supply/license audit. Existing fourth-edition notices and the recorded stale-root-license discrepancy remain binding. Independent renderer mathematics, browser, native-reader, educator and assistive-technology review remain. All five assigned works remain incomplete.

## Revision and final input QA

The revision stage actually reread all six previously identified complete canon paragraphs (C01/C02/C05/C06/C09/C10/C11); final QA reread R2 line 27 and R3 lines 31, 25 and 38 (C05/C09/C10/C11). Separate receipts record the successful displayed reading and unit-specific decisions. One initial revision stdout attempt failed on encoding before displaying text and is not counted as reading evidence. No canon files were rewritten. The machine clock's observed QA timestamp is retained as returned, not backfilled to the drafting hour.

Revision uses `سچ / جھوٹ` consistently for truth values while retaining `غلط` for technical mistakes; the original bridge explains that a false statement need not be an intentional lie. `اسیں`, `سکدے او`, `چیتے رکھو` and `کیوں جے` keep Punjabi syntax. Corrected duplicated `ہووو`; revised the Jack/Jill answer to negate the whole conjunction rather than imply neither passed. The strict-less-than feedback keeps its original emphasis before the final `y` slot. Original terminology is provisional, and Urdu/English access labels do not replace Punjabi explanations. The source's explicit request to write in English remains; the Indonesian switch to its own language is not silently adopted.

The suite reconstructs the canonical selection and both include expansions, checks the actual source-lock role/pin tuples, exact source and metadata files, full raw macro text, all 559 key/order/inline-event/numeral contracts, 337 untouched TeX owners (123 unique raw forms), 40 term occurrences, 8 cross-references, 17 XML IDs, 18 exercise anchors, source child/own-text/tail ledgers and all choice/match/feedback/solution/cache associations. Cache list `a.` labels remain patterns, not IDs. It also checks all eight table shapes and exact data; the negation table's fourth, single empty cell is retained. Independent handwritten Boolean truth-row expectations, arithmetic for static/cached examples, a two-person/two-time countermodel, and the exact cached 5×5 matrix are checked without a TeX converter or upstream runtime.

Input tests caught an unescaped less-than sign inside the separately original interval note and an earlier inline-emphasis ordering issue; both were corrected. JSON duplicate-key rejection is enabled. Detached tests alter only in-memory copies: digits, table values, term identity, slot order/nesting, isolates, answers, source text/math/owners, link targets, choice flags, table shape, cache seed/answer/arithmetic/image metadata, macro direction, note origin/removal/arithmetic and randomized-coverage overreach. Seals guard the reviewed target and original text but do not prove linguistic accuracy. The source inventory and QA use the same authored inventory function, explicitly not independent parser implementations.

Both source-witness hashes use only CRLF→LF normalization in memory. The main witness is 66,000 logical-LF bytes with SHA-256 `1fce081a82dc8d22fa044da96fe8ba309fe4b0a4db7ab9835b0cf7f63e280009`; the six-record cache witness is 18,484 bytes with SHA-256 `1c8947d8d695deca514e0e6952e6b8ce96938cffb02bb19e15feb314d769bf7c`. The latter is a new wrapper with serialized selected source record trees, not a claimed lexical slice of the 729,050-byte full pinned cache. Metadata, static/PG text and all selected record trees remain exact.

Production requirements remain: preserve every raw TeX string and inert macro, add only a scoped nonexecuting converter after an observed-grammar review, independently bind new logic/quantifier/negative-spacing/text/fence trees, keep source-error notices visible from the affected tasks, retain source-order association even when source randomize attributes exist, show pending forward references honestly and make no remote grading/all-randomized-versions promise. There are no source image assets to copy and no justification for running PG/Perl/PreTeXt/TeX or contacting an exercise server.
