# PNB-013 — complete m49306 production record (in progress)

This is a complete-source-module checkpoint inside the still-active five-work assignment, not a finished book, release, native-language certification or training corpus. PNB-012 is frozen and is not changed by this unit.

## Exact witnessed scope

The entire pinned canonical `m49306/index.cnxml`, *Rates of Change and Behavior of Graphs*, is retained: root title; complete metadata with four objectives; every instructional section, Key Equations, Key Concepts and all Section Exercises; and all nine trailing glossary definitions. The collection-next module is `m49308`.

Canonical raw SHA-256 is `043afbc674a470470bf2288160f0136e32fe72740f76a73c2d52ba4a95b29370`; its CRLF→LF-equivalent complete witness is `87e3bea0a14b4f487e0a5428b8138a644b0630b692d02c5a0e348e26ae3e6806`. Indonesian comparison SHA-256 is `d14cddfd180339f2958f47d66e30c90709c2119c3f2074165d3bf5c4dfef6c9e`. There are exactly **3,436 non-document elements, 438 IDs, 353 source block keys, 254 MathML trees, 24 media/JPEGs, 19 figure wrappers, five CALS tables, 37 links and one footnote**.

The source has 61 exercises and 38 source-supplied solutions overall. Fourteen instructional exercises all have solutions. Section Exercises contain 47 exercises, 24 supplied solutions and 23 exercises with no supplied solution. Those 23 answers remain absent; no answer may be inferred from a pattern or invented.

## Canon and comparison actually consulted

- Source analysis: `PNB-013-study-next-unit-20260901T142834088775Z.json`, C01/C02/C03/C04/C05/C06/C09/C10/C11. These passages were displayed and read before drafting. They inform ability phrasing, direct instructions, ordered sequence, plural agreement, purpose, explicit alternatives, reminders, qualified claims and reasons. Their inherited `application` fields are prompts rather than proof about this unit, and this study receipt is not a draft- or revision-stage receipt.
- Part B has explicit draft/revision/QA readings in `PNB-013-part-b-draft-20260901T155502197593Z.json`, `PNB-013-part-b-revision-20260901T161106552203Z.json` and `PNB-013-part-b-qa-20260901T161640022860Z.json`. The older QA receipt contains a receipt-only typo in C04's paragraph hash; it is preserved rather than silently rewritten.
- No contemporaneous Part A or whole-unit draft/revision canon receipt exists. No receipt was fabricated, backdated or retroactively claimed; this evidence gap remains explicit.
- Whole-unit QA reread the displayed R1/R2/R3 passages after all 353 keys existed. `PNB-013-qa-20260901T185936943200Z.json` records the valid C04 hash `2b8bfffa5cc63fcf6c267d51dbed8c23de3ec997bc4c0a6bf6299ef78d209e49` and supersedes the older receipt for PNB-013 QA evidence. The actual C04 paragraph informed the attached plural `بدلدے` correction; C02 supported direct instructions; C05/C06 kept purpose and alternatives explicit; C09/C10/C11 guided reminders, qualifications and reasons. This remains prose-register evidence, not specialist-term authority.

The English and Indonesian titles, objectives, main rate-of-change/increasing/decreasing/extrema passages, verbal Q&A and all nine glossary meanings were actually displayed and compared at source analysis. Indonesian is a comparison witness, not authority to replace canonical content. Its ten SVG redraws are not canonical image replacements. Canon passages are twelve prose loci from three essays; they are not a mathematical terminology corpus or native-speaker certification.

## Structure and renderer contract

All 353 keys retain canonical preorder. Inline slots are source-derived `{{math:N}}`, `{{term:N}}`, `{{link:N}}` and `{{child:N}}`. The one actual `<newline/>` must become a real line break, not disappear or become invented prose. Four circled source tokens map in order to isolated `(a)`–`(d)`. Twenty-four emphasis nodes retain their actual default-bold or italic effect. Twenty-one identified inline terms remain independently keyed; nine glossary terms use their definition-owner keys.

All 254 MathML trees remain exact source trees. Seven Indonesian trees differ only through translated `mtext`; canonical English `mtext` remains in the source-derived tree and any Punjabi aid must be a separately labeled key. Seventy-six trees end in source punctuation-like leaves. Ordinary terminal `mo` period/comma and numeric `mn` values such as `4.`, `−2.`, `16.` and `15.` need reversible owner/slot/path ledgers. A numeric token must never be stripped merely because it ends in a period; owner-bound source position and unchanged numeric meaning are required.

The five source tables are exactly 2×9, 2×9, 1×2, 10×2 and 10×2. None has a source `thead`; semantics must follow actual bold/header content rather than inventing a table orientation. Tables 01 and 02 have two genuine row headers in their first columns and eight data values per row. The one-row equation table has a row label and a formula data cell. Tables 03 and 04 have two first-row column headers and nine ordinary data rows. All original row order, colspecs, alignment attributes and data values stay source-derived. The `eip-id1165135358784` summary is only `..`, so a useful aria label is explicitly original while the source summary remains separately preserved.

The 37 links comprise 36 local targets and one external `https://openstax.org/l/aroc` label. The single footnote retains the original EIA URL and access date `3/5/2014` with LTR isolation and endnote navigation. It is a dated source witness, not a current data/API endorsement.

Twenty-four unchanged canonical JPEGs were actually viewed at original detail in source order. Fourteen Indonesian comparison assets are byte-identical JPEGs and ten are separate SVG redraws. Original asset dimensions range from 249×251 through 976×568; wide images must scroll locally on narrow screens without causing page overflow. Five source media occur directly inside problems or a solution rather than figure wrappers, so `figure_id` is optional while `media_id` is always required.

## Material source discrepancies and precision notes

1. `Table_01_03_03` source summary says `(2004, 243)`, while its actual 2004 cell is **249**. The exercise asks for the 2001→2004 average change and has no supplied source solution. Preserve the false summary as `data-source-summary`, preserve actual cell 249, use an explicitly original aria correction based on the real 10×2 table, and do not invent the missing exercise answer.
2. The canonical glossary meanings `fs-id1165135412035`/`fs-id1165135412046` define local maximum/minimum as an **input value where direction changes**. Main prose `fs-id1165134272749` distinguishes that input location from the function value, while formal inequalities `fs-id1165135389881` define a neighborhood extremum. Indonesian silently replaces the glossary with function-value/neighborhood definitions. Preserve every English witness faithfully and add a separate original precision note; do not substitute Indonesian wording into the translated glossary.
3. The average-rate formula divides by `x₂−x₁` but the displayed definition does not explicitly state `x₂≠x₁`. Keep the source formula unchanged. Put the nonzero-denominator condition only in a clearly marked original precision note.
4. Source paragraph `fs-id1165131833490` says “attains it highest”; translate the intended possessive meaning without pretending the English wording is grammatical evidence.
5. The toolkit JPEG `fs-id1165135186344` visibly prints the constant formula as `f(x) =` with the constant missing. Preserve the unchanged pixels and faithful generic source alt; an original accessible description must disclose the blank rather than silently claiming that the image displays `f(x)=c`.
6. Media `fs-id1165134080979` is called a reciprocal-function graph by its generic source alt. The actual pixels show two upper local peaks at about `(1,0)` and `(4,0)`, both sides falling toward a vertical dashed line `x=3`, and outer arrows falling away. Describe those pixels without inventing an exact formula.
7. The source IDs/file names around Figure015/014 cross: media `fs-id1165137459600` belongs to `Figure_01_03_015` but uses `...014-7451.jpg`; media `fs-id1165134153022` belongs to `Figure_01_03_014` but uses `...015.jpg`. Preserve the actual source mapping, not a name-based swap.
8. Many source alts say only “graph of a polynomial/cubic/reciprocal function” or “table showing...” and omit visible extrema, axes and intervals. Faithful translated alts stay available separately; enhanced descriptions are original accessibility additions linked to a visible advisory.
9. Calculator image `fs-id1165134377142` has two panels: a positive-branch minimum near `(2.4494898,1.6329932)` and a negative-branch maximum near `(−2.449491,−1.632993)`. Source alt is generic. Preserve the values as displayed and do not claim exactness beyond the calculator estimate.
10. Gas prices 2005–2012, product sales 1998–2006, town population 2000–2008 and calculator screenshots are historical/hypothetical source examples, not current prices, policy or technology recommendations. Units must remain attached: dollars per gallon, miles/hours, millions of dollars, thousands of people, gallons/minute and similar source units.
11. Verbal exercise `fs-id1165135536272` gives increasing behavior on `(a,b)` and decreasing behavior on `(b,c)` but states nothing about whether `f(b)` exists or its value. That alone does not force a local extremum at `b`. Preserve the unsolved source question and add a separate original qualification; do not silently add continuity or a value at `b`.
12. Several symbolic average-rate examples divide by a parameter difference without stating the exclusion that makes the denominator nonzero: Example06 simplifies by `a` and therefore requires `a≠0`; its Try It analogue requires `a≠5`; later difference-quotient/rational exercises carry their own domain exclusions. Preserve the source work and put each missing condition in original precision support, never inside the translated source answer.

## Image observations for future accessible descriptions

- `fs-id1165137838377`: upward parabola with vertex `(1,0)`.
- `fs-id1165137387448`: same graph with secant endpoints `(−1,4)` and `(2,1)`, `Δg(t)=−3`, `Δt=3`.
- `fs-id1165135390884` and `fs-id1165137459600`: cubic-like graph with extrema `(−2,16)` and `(2,−16)`, increasing/decreasing intervals visibly labeled.
- `fs-id1165137724379`: schematic local maximum at input `b`, output `f(b)`, increasing left and decreasing right.
- `fs-id1165134106001`: local minimum near `(1,−1)`, local maximum near `(3,1)`, then another shallow minimum near `(4,0.5)`.
- `fs-id1165137888935`: reciprocal-like two branches with a positive-branch minimum and negative-branch maximum.
- `fs-id1165134043615`: labeled extrema `(−1,28)` and `(5,−80)`.
- `fs-id1165135516800`: cubic with a local minimum near `(−1,−2)` and maximum near `(1,2)`.
- `fs-id1165134153022`: closed domain roughly `[−1,2]`, absolute minimum `(0,−2)` and maximum `(2,2)`.
- `fs-id1165134360890`: closed endpoints near `(−2.5,12)` and `(3,−10)`, local highs near `16`, low `(0,0)`.
- `fs-id1165135581139`: successive local maxima near `(1,5)`/`(5,7)` and minima near `(3,1)`/`(7,3)`.
- `fs-id1165133213923`: absolute-value V with vertex `(1,−3)`.
- `fs-id1165137897946`/`fs-id1165135422552`: cubic graphs with opposite end behavior and approximate extrema.
- `fs-id1165134373469`: cubic-like graph, maximum near `(−3,50)` and minimum near `(3,−55)`.
- `fs-id1165135404188`: closed finite graph, left endpoint near `(−8,−220)`, local max near `(−3,50)`, min near `(4,−55)`, right endpoint near `(7,150)`.
- `fs-id1165134298977`: calculator maximum estimate `x=1.3333324`, `y=5.1851852`.
- `fs-id1165135517159`: decreasing exponential amount from about 16 milligrams near day 1 toward 5 milligrams near day 19.

## Current state

The exact complete source excerpt, source-derived manifest, existing component notice rows, all 24 unchanged admitted JPEG copies, complete 353-key translation, renderer and independent failure-driven QA exist. Freeze requires two explicit prepare/assemble/build/check-only cycles with matching hashes, followed by the non-check deterministic-build assertion and `qa/structural-013.json`; exact run totals and hashes belong in that generated receipt rather than being copied here as mutable prose. Parent browser review remains separate and pending. No shared CSS, shared attribution, prior unit or source pin has been changed.

The final source-fidelity language passes corrected material issues without changing any placeholder, numeral, MathML tree, source ID or source order: attached plural `بدلدے`; Punjabi `ایہو جیہا عدد` in both reciprocal-function questions; action-equivalent `اوہنے … پیٹرول بھریا سی`; plural `اہم خیالاں`; the moon exercise's source distinction between *average velocity* (`اوسط رخ والی رفتار`, with separately labeled Urdu `اوسط سمتی رفتار`) and earlier *average speed* (`اوسط رفتار`); Punjabi `ایس طرح`/`ایسیاں قدراں`; consistent page/link terminology `بدلاوا`/`ورتارا`; isolated visible English in the original bridge and table-summary advisory; and localized Punjabi image descriptions. QA carries detached regressions for these correction classes and explicitly preserves the canon-receipt limitation above.
