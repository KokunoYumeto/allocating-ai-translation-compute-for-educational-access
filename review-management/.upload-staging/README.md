# Incomplete upload staging

This directory is temporary transport state, not a reader or a usable ZIP.
The `.part` file is only a byte prefix of one reviewed artifact. Do not download,
open, build, cite, or treat this partial branch as a complete handoff. The final
commit removes this directory and installs the exact reviewed blob at its required path.
